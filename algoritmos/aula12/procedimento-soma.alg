algoritmo "procedimento-soma"
var
   x, y: inteiro
procedimento soma(a, b: inteiro)
inicio
      escreval("A soma entre ", a, " e ", b, " e", a + b)
fimprocedimento
inicio
      x <- 5
      y <- 3
      soma(x, y)
fimalgoritmo
